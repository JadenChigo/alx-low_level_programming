#include "main.h"

/**
 * _strlen - it returns the lengrh of a string
 * @s: the string
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int length = 0;
	int num;
	while (s[length] != '\0')
	{
		length++;
	}
	for (num = 0; num <= length; num++)
	{
u
}
