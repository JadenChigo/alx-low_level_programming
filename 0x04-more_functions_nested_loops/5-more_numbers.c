#include "main.h"

/**
 * more_numbers - prints 10 times the numbers (1 to 14)
 * Return: Always 0
 */
void more_numbers(void)
{
	char m;

	for (m = '0'; m < '15'; m++)
	{
		while (m = '10')
		{
			_putchar('\n');
			m++;
		}
		_putchar(m);
	}
	_putchar('\n');
}
