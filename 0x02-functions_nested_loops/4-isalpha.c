#include "main.h"
/**
 *  _isalpha - checks for an alphabet
 *  @c: is the variable used
 *  Return: 1 if c is an alphabet, 0 if it isn't
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 122)
		return (1);
	}
	else
	{
		return (0);
	}
}
